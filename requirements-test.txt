# Minimal requirements for running unit tests
# Does not include database drivers or production dependencies

pytest>=7.0.0
pytest-cov>=4.0.0
pyyaml>=6.0
pandas>=2.0.0
python-dotenv>=1.0.0
loguru>=0.7.0
